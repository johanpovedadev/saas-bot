# Reglas del repo

El bot multi-tenant vive en `bot-wasap/`. Las reglas de validación (qué probar antes de
tocar código compartido, cómo agregar un negocio nuevo, aislamiento de `.env` por tenant)
están en [`bot-wasap/COPILOT_RULES.md`](bot-wasap/COPILOT_RULES.md).
